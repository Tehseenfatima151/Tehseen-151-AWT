PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=foodify_db
JWT_SECRET=foodify-secret-key
STRIPE_SECRET_KEY=
# Card totals below this (PKR) are rejected by Stripe (~$0.50 USD minimum). Default 200.
STRIPE_MIN_ORDER_PKR=200
